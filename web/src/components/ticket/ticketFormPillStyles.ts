// Shared by every pill trigger (Type/Category/Developer/Tester/project) in the metadata row.
export const pillTriggerClass =
  "inline-flex h-7 shrink-0 items-center gap-1.5 rounded-md border border-input px-2 text-xs font-medium text-foreground transition-colors duration-150 ease-standard hover:bg-accent focus-visible:outline-none focus-visible:ring-[3px] focus-visible:ring-ring/30";

// Shared by every option row inside a pill's popover; matches BoardCreateMenu's list-item convention.
export const menuItemClass =
  "flex items-center gap-2 rounded-md px-2.5 py-1.5 text-left text-xs text-foreground outline-none transition-colors duration-150 ease-standard hover:bg-accent/60 focus-visible:bg-accent/60";
