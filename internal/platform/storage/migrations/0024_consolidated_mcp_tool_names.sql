-- The MCP tools were consolidated (ADR 0068), so stored play instructions name tools that no longer exist.
-- A built-in play still holding its seeded text gets the new seeded text; an edited one gets the old names swapped.

UPDATE plays SET instructions = 'Confirm your Nexul access by reading the ticket with `ticket_get`, which also lists its linked pull requests and branches. Work in the project checkout you are running in. Create a branch named `<ticket key>-<short-slug>` from the default branch, implement the fix, run the project''s tests and linters, and commit. Push the branch, open a pull request whose title starts with the ticket key, then link both to the ticket with `ticket_update`, passing `link_branch` and `link_pr`. Do not merge unless a memory selected for this run explicitly permits merging; if one does, merge once checks pass. Reply with what changed, how it was verified, and the PR link. If you cannot complete the fix, say what blocked you instead of opening a partial PR.'
WHERE label = 'Fix with AI' AND instructions = 'Confirm your Nexul access by reading the ticket with `ticket_get` and its links with `ticket_get_links`. Work in the project checkout you are running in. Create a branch named `<ticket key>-<short-slug>` from the default branch, implement the fix, run the project''s tests and linters, and commit. Push the branch, open a pull request whose title starts with the ticket key, then link the branch and the PR to the ticket with `ticket_link_branch` and `ticket_link_pr`. Do not merge unless a memory selected for this run explicitly permits merging; if one does, merge once checks pass. Reply with what changed, how it was verified, and the PR link. If you cannot complete the fix, say what blocked you instead of opening a partial PR.';

UPDATE plays SET instructions = 'Confirm your Nexul access by reading the doc with `doc_get`. Read the doc''s project with `project_get`, which lists its status columns and ticket types. Search existing tickets with `ticket_list` and a `query` so you do not duplicate work already tracked. Split the doc into tickets a developer could pick up independently: one outcome per ticket, a title under eighty characters, a body with context, acceptance criteria, and a pointer to the doc section it came from. Create each with `ticket_create`, passing the doc id so the ticket links back. Put them in the backlog column. Reply with the list of tickets created and anything in the doc you deliberately did not turn into a ticket.'
WHERE label = 'To tickets via AI' AND instructions = 'Confirm your Nexul access by reading the doc with `doc_get`. List the doc''s project''s columns with `status_list` and its ticket types. Search existing tickets with `ticket_search` so you do not duplicate work already tracked. Split the doc into tickets a developer could pick up independently: one outcome per ticket, a title under eighty characters, a body with context, acceptance criteria, and a pointer to the doc section it came from. Create each with `ticket_create`, passing the doc id so the ticket links back. Put them in the backlog column. Reply with the list of tickets created and anything in the doc you deliberately did not turn into a ticket.';

UPDATE plays SET instructions = 'Run this project''s interview, the conversation that records its rules for agents; the project and the answers it already records are named below. Start by calling `memory_create` with the project id and `kind` `interview`: it returns the interview memory, created from the workspace''s Interview template the first time. If the memory already holds rules, this is a re-run: amend it, never start over. Ask first what has changed, keep every rule that still holds, and change only what the answers change. Ask one question at a time with your question tool, never a batch, and give every question your recommended answer as its first option, labelled (Recommended), so the person can accept it or type their own. Your first question asks whether to scan the codebase for answers first. If they say yes, read the checkout you are running in (manifests and lockfiles, CI config, linter and formatter config, tests, README, docs and decision records), draft an answer for each category, then grill them on it: one question per finding, saying what you found and where, until each is confirmed or corrected. Never record a finding they have not confirmed. Work through the categories in the interview''s headings in order. Do not ask again what the project already records, such as where its tests live, unless the person changes it; record a change there with `project_update` and `tests_location` as well. Save the interview with `memory_update` after each category, passing its id and the full markdown body, so progress survives a stop. Write rules, not a transcript: short imperative lines under each heading, with no questions, answers, or narration. Replace each heading''s prompt line with its rules, and leave out a heading the project has no rule for. The body is capped at 8,000 characters of markdown and every agent turn in the project carries it in full, so keep it well under the cap: tighten wording and drop what a linter or the code already enforces. Reply with a short summary of what the interview now says and what this run changed.'
WHERE label = 'Interview' AND instructions = 'Run this project''s interview, the conversation that records its rules for agents; the project and the answers it already records are named below. Start by calling `memory_create_interview` with the project id: it returns the interview memory, created from the workspace''s Interview template the first time. If the memory already holds rules, this is a re-run: amend it, never start over. Ask first what has changed, keep every rule that still holds, and change only what the answers change. Ask one question at a time with your question tool, never a batch, and give every question your recommended answer as its first option, labelled (Recommended), so the person can accept it or type their own. Your first question asks whether to scan the codebase for answers first. If they say yes, read the checkout you are running in (manifests and lockfiles, CI config, linter and formatter config, tests, README, docs and decision records), draft an answer for each category, then grill them on it: one question per finding, saying what you found and where, until each is confirmed or corrected. Never record a finding they have not confirmed. Work through the categories in the interview''s headings in order. Do not ask again what the project already records, such as where its tests live, unless the person changes it; record a change there with `project_set_tests_location` as well. Save the interview with `memory_update` after each category, passing its id, title, when-to-use, and the full markdown body, so progress survives a stop. Write rules, not a transcript: short imperative lines under each heading, with no questions, answers, or narration. Replace each heading''s prompt line with its rules, and leave out a heading the project has no rule for. The body is capped at 8,000 characters of markdown and every agent turn in the project carries it in full, so keep it well under the cap: tighten wording and drop what a linter or the code already enforces. Reply with a short summary of what the interview now says and what this run changed.';

UPDATE plays SET instructions = 'Test this ticket the way a tester would, then pass or fail it. Read it with `ticket_get`, which also carries its links and where to test; its acceptance criteria are what you test against. Follow the testing strategy in this project''s interview memory, which comes with this run. With no interview, check each criterion on the live URL and run the tests the project already has, and add none. Where to test is the `test_target` in that result. Test nowhere else: never production, and never anything that shares production''s services. If its url is empty, the only place to test is production: stop without passing or failing the ticket, and reply that it needs a deploy branch on its own network. Open the url and check it against each acceptance criterion in turn, noting what you did and what you saw. Run the project''s tests. List its repositories with `project_get`: when one has the role tests, run them from that repository, cloning it if the checkout you are running in is not it; otherwise run them in this checkout on the ticket''s linked branch. Where the interview calls for an automated end-to-end suite, add or extend a test covering the ticket''s acceptance criteria, run it against the url, commit it, and push: to the ticket''s linked branch, or in a tests repository to a branch named `<ticket key>-<short-slug>` with a pull request whose title starts with the ticket key. Then record the result exactly as a person would. If every criterion holds and the tests pass, call `ticket_test_report` with `outcome` `pass`. Otherwise call it with `outcome` `fail` and the bug template filled: the steps to reproduce, the expected result the criterion promises, and the actual result you saw. It posts them to the ticket''s thread and moves the ticket back to progress. Reply with each criterion and whether it held, the tests you ran and added, and the result you recorded. If you cannot reach the url or run the tests, say what blocked you instead of passing or failing the ticket.'
WHERE label = 'Test with AI' AND instructions = 'Test this ticket the way a tester would, then pass or fail it. Read it with `ticket_get` and its links with `ticket_get_links`; its acceptance criteria are what you test against. Follow the testing strategy in this project''s interview memory, which comes with this run. With no interview, check each criterion on the live URL and run the tests the project already has, and add none. Get where to test with `ticket_get_test_target`. Test nowhere else: never production, and never anything that shares production''s services. If its url is empty, the only place to test is production: stop without passing or failing the ticket, and reply that it needs a deploy branch on its own network. Open the url and check it against each acceptance criterion in turn, noting what you did and what you saw. Run the project''s tests. List its repositories with `project_list_repos`: when one has the role tests, run them from that repository, cloning it if the checkout you are running in is not it; otherwise run them in this checkout on the ticket''s linked branch. Where the interview calls for an automated end-to-end suite, add or extend a test covering the ticket''s acceptance criteria, run it against the url, commit it, and push: to the ticket''s linked branch, or in a tests repository to a branch named `<ticket key>-<short-slug>` with a pull request whose title starts with the ticket key. Then record the result exactly as a person would. If every criterion holds and the tests pass, call `ticket_test_pass`. Otherwise call `ticket_test_fail` with the bug template filled: the steps to reproduce, the expected result the criterion promises, and the actual result you saw. It posts them to the ticket''s thread and moves the ticket back to progress. Reply with each criterion and whether it held, the tests you ran and added, and the result you recorded. If you cannot reach the url or run the tests, say what blocked you instead of passing or failing the ticket.';

UPDATE plays SET instructions = replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(replace(instructions, '`access_list_grants`', '`permission_overwrite_list`'), '`access_set_grants`', '`permission_overwrite_update`'), '`account_disable`', '`account_update` with `status` `disabled`'), '`account_reactivate`', '`account_update` with `status` `active`'), '`account_remove`', '`account_delete`'), '`account_restore`', '`account_update` with `status` `active`'), '`account_whoami`', '`account_get`'), '`automation_get`', '`automation_list` with an `id`'), '`automation_mint_token`', '`automation_token_create`'), '`automation_revoke_token`', '`automation_update` with `revoke_token`'), '`automation_set_enabled`', '`automation_update` with `enabled`'), '`automation_update_config`', '`automation_update` with `config_values`'), '`category_clear_ticket`', '`ticket_update` with an empty `category_id`'), '`category_create`', '`project_update` with `categories`'), '`category_delete`', '`project_update` with `categories`'), '`category_get`', '`project_get`'), '`category_list`', '`project_get`'), '`category_move_ticket`', '`ticket_update` with `category_id`'), '`category_rename`', '`project_update` with `categories`'), '`category_reorder`', '`project_update` with `categories`'), '`chat_list_conversations`', '`conversation_list`'), '`chat_list_messages`', '`message_list`'), '`chat_post_message`', '`message_post`'), '`computer_mcp_token_get`', '`computer_list`'), '`computer_mcp_token_mint`', '`computer_mcp_token_create`'), '`computer_mcp_token_revoke`', '`computer_mcp_token_delete`'), '`computer_setup_confirm`', '`computer_setup_update` with `confirmed` true'), '`computer_setup_confirm_provider`', '`computer_setup_update` with a `provider` and `confirmed` true'), '`computer_setup_get`', '`computer_list`'), '`computer_setup_retry_provider`', '`computer_setup_run` with a `provider`'), '`computer_setup_start`', '`computer_setup_run`'), '`computer_setup_unconfirm`', '`computer_setup_update` with `confirmed` false'), '`computer_setup_unconfirm_provider`', '`computer_setup_update` with a `provider` and `confirmed` false'), '`computer_tunnel_create`', '`computer_create`'), '`computer_tunnel_status_get`', '`computer_list` with an `id`'), '`deploy_list_by_service`', '`deploy_list` with a `stack_id`'), '`deploy_list_by_status`', '`deploy_list` with a `status`'), '`deploy_log`', '`deploy_get`'), '`dns_check_propagation`', '`dns_record_list` with `check_propagation`'), '`dns_create_record`', '`dns_record_create`'), '`dns_delete_record`', '`dns_record_delete`'), '`dns_exposure_list`', '`exposure_list`'), '`dns_gateway_create`', '`gateway_create`'), '`dns_gateway_delete`', '`gateway_delete`'), '`dns_gateway_list`', '`gateway_list`'), '`dns_get_service_hostname`', '`exposure_list`'), '`dns_list_records`', '`dns_record_list`'), '`dns_list_service_hostnames`', '`exposure_list`'), '`dns_list_zones`', '`dns_zone_list`'), '`dns_provision_reverse_proxy`', '`gateway_create`'), '`dns_remove_service_hostname`', '`exposure_delete`'), '`dns_set_service_hostname`', '`exposure_create`'), '`dns_tunnel_get`', '`dns_tunnel_list` with an `id`'), '`dns_tunnel_provision_agent`', '`gateway_create`'), '`dns_tunnel_rotate`', '`dns_tunnel_update` with `rotate_credentials`'), '`dns_tunnel_route`', '`dns_tunnel_update` with a `hostname`'), '`dns_update_record`', '`dns_record_update`'), '`dns_verify_credentials`', '`dns_zone_list`'), '`doc_archive`', '`doc_update` with `archived` true'), '`doc_restore`', '`doc_update` with `archived` false'), '`doc_search`', '`doc_list` with a `query`'), '`doc_thread_get`', '`message_list` with a `doc_id`'), '`git_get_change_context`', '`pull_request_get`'), '`git_get_pr`', '`pull_request_get`'), '`git_list_prs`', '`pull_request_list`'), '`instance_upgrade_status`', '`instance_get`'), '`interview_thread_get`', '`message_list` with a `project_id`'), '`invitation_revoke`', '`invitation_delete`'), '`memory_clone`', '`memory_create` with `clone_from_id`'), '`memory_create_interview`', '`memory_create` with `kind` `interview`'), '`memory_list_versions`', '`memory_get`'), '`memory_revert`', '`memory_update` with `revert_to_version`'), '`mention_resolve`', '`mention_search` with `refs`'), '`notification_mark_all_read`', '`notification_update` with `all` true'), '`notification_mark_read`', '`notification_update`'), '`play_list_runs`', '`trail_list`'), '`play_run_answer`', '`trail_update` with an `answer`'), '`play_run_get`', '`trail_list` with an `id`'), '`play_run_stop`', '`trail_update` with `stop`'), '`project_add_repo`', '`project_update` with `add_repos`'), '`project_delete_impact`', '`project_get`'), '`project_list_repos`', '`project_get`'), '`project_move_ticket`', '`ticket_update` with a `project_id`'), '`project_remove_repo`', '`project_update` with `remove_repos`'), '`project_rename`', '`project_update` with a `name`'), '`project_reorder`', '`project_update` with a `position`'), '`project_set_tests_location`', '`project_update` with `tests_location`'), '`review_get`', '`ticket_get`'), '`review_list_by_ticket`', '`ticket_get`'), '`runner_list`', '`machine_list`'), '`runner_queue`', '`machine_list`'), '`service_list`', '`stack_get`'), '`stack_rollback`', '`stack_deploy` with `rollback`'), '`status_create`', '`project_update` with `statuses`'), '`status_delete`', '`project_update` with `statuses`'), '`status_list`', '`project_get`'), '`status_rename`', '`project_update` with `statuses`'), '`status_reorder`', '`project_update` with `statuses`'), '`ticket_add_blocker`', '`ticket_update` with `add_blocker_ids`'), '`ticket_add_label`', '`ticket_update` with `add_labels`'), '`ticket_get_links`', '`ticket_get`'), '`ticket_get_test_target`', '`ticket_get`'), '`ticket_get_ticket_links`', '`ticket_get`'), '`ticket_label_colors`', '`project_get`'), '`ticket_link_branch`', '`ticket_update` with `link_branch`'), '`ticket_link_pr`', '`ticket_update` with `link_pr`'), '`ticket_list_all_labels`', '`project_get`'), '`ticket_list_blocked`', '`ticket_list` with `blocked_only`'), '`ticket_list_labels`', '`ticket_get`'), '`ticket_remove_blocker`', '`ticket_update` with `remove_blocker_ids`'), '`ticket_remove_found_in`', '`ticket_update` with `clear_found_in`'), '`ticket_remove_label`', '`ticket_update` with `remove_labels`'), '`ticket_search`', '`ticket_list` with a `query`'), '`ticket_set_developer`', '`ticket_update` with `developer`'), '`ticket_set_found_in`', '`ticket_update` with `found_in_id`'), '`ticket_set_label_color`', '`project_update` with `label_colors`'), '`ticket_set_tester`', '`ticket_update` with `tester`'), '`ticket_set_type`', '`ticket_update` with `type_id`'), '`ticket_test_fail`', '`ticket_test_report` with `outcome` `fail`'), '`ticket_test_pass`', '`ticket_test_report` with `outcome` `pass`'), '`ticket_type_create`', '`project_update` with `ticket_types`'), '`ticket_type_delete`', '`project_update` with `ticket_types`'), '`ticket_type_list`', '`project_get`'), '`ticket_type_rename`', '`project_update` with `ticket_types`'), '`ticket_type_set_template`', '`project_update` with `ticket_types`'), '`ticket_update_status`', '`ticket_update` with `status_id`'), '`topology_add_edge`', '`topology_update` with `add_edges`'), '`topology_add_node`', '`topology_update` with `add_nodes`'), '`topology_remove_edge`', '`topology_update` with `remove_edge_ids`'), '`topology_remove_node`', '`topology_update` with `remove_node_ids`')
WHERE instructions LIKE '%`%';
